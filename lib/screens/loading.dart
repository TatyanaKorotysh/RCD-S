import 'package:flutter/material.dart';
import 'package:rcd_s/screens/localConnect.dart';

class Leading extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    //if connection true => Devices
    //else StartScreen

    return LocalConnect();
  }
}
